import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerGenerativeApisTools(_server: McpServer): void {
	// Tool implementations will be added by the product SDD spec
}
