# Initial analysis record

Historical read-only analysis performed during the 2026-09-06 retrofit.
Counts describe that pass, not the final state. See analysis.md and ../retrofit-compliance.md.
No historical reviewer statement grants a governance exemption.

| ID | Severity | Finding | Recommendation |
| --- | --- | --- | --- |
| C1 | CRITICAL | Principle II MUST ('Every feature MUST have a spec before implementation begins') was violated: implementation shipped in 0.4.0 (f46a252, 2026-09-05) and the spec/plan/tasks were retrofitted on 2026-09-06. The Constitution Check row labels this PASS (retrofitted), which reinterprets the principle; Complexity Tracking L103 records the justification but no explicit exception grant/approver. | Relabel the II row as 'EXCEPTION (documented)' rather than PASS, and add approver + date to the Complexity Tracking row per Governance §Compliance ('Violations MUST be documented with justification if exception granted'). This cannot be cured retroactively, only recorded honestly; keep T054 open until done. |
| C2 | CRITICAL | Principle VIII 'No exceptions, no exclusions' is violated: vitest coverage config excludes src/main.ts. plan.md L21 asserts '100% coverage with no exclusions' and L37 marks VIII PASS. Verified: 100% lines/branches is reported only because main.ts is excluded (pre-existing since a71f26c, but the plan misstates it — silent dilution). | Remove the src/main.ts exclusion and cover it (e.g. export the bootstrap so the catch/exit path is testable with a mocked startServer), or record an explicit approved exception in Complexity Tracking. Correct plan.md L21 and L37 to state the actual exclusion. |
| C3 | CRITICAL | Principle IV MUSTs (structured JSON logging for all operations; health status) are unmet. The feature adds new operation paths (search/describe/read/call, executeOperation) that log nothing, so the plan's 'Not introduced by this feature' is inaccurate for the new code. Justification and follow-up T056 exist (Governance form is followed, not silent), but no exception grant/approver is recorded and T056 has no owner or target release; gate result L39 still says 'proceed'. | Either add one structured stderr JSON line per gateway dispatch (op id, outcome, duration, no params) within this feature, or record an explicit exception (approver, date, target release) in Complexity Tracking. Open a tracked issue for T056 and reword L33 to acknowledge the gateway path is also un-logged. |
| C4 | CRITICAL | Principle VII MUST 'Scaleway API responses MUST be validated against expected types' is unmet in 49/50 areas and FR-009 requires the gateway to return handler results unchanged, so read/call inherit unvalidated responses. Documented as PARTIAL and tracked (T057), but no exception grant is recorded and T057 has no owner, scope milestone or target. | Record an explicit exception (approver/date/target) in Complexity Tracking and scope T057 with a first milestone (e.g. the 20 SC-004 operations). The no-`any` claim for gateway/shared code was verified and holds. |
| C5 | CRITICAL | Principle I MUST 'Tool descriptions MUST ... include usage examples': 679 of 724 operation descriptions now served through describe/search carry no example (scan of the built registry: 45/724 contain an example); enforcement test covers only autoscaling, containers and deprecated cockpit. The plan marks I as PASS with the gap 'outside this feature', although describe is now the delivery mechanism for these descriptions. | Relabel I as PARTIAL with an explicit exception record; turn T058 into a tracked issue with an incremental target (extend description-examples.test.ts as each area is touched) or surface a generated example in describe. |
| C6 | HIGH | Principle III MUST 'Tool schemas MUST be defined in contracts/ before any code' and 'Gateway meta-tools MUST be specified in contracts/ before implementation' cannot be verified: contracts/gateway-tools.md, src/gateway/* and the constitution v1.2.0 amendment (T003) that authorizes gateway meta-tools all landed in the same commit f46a252. The amendment legitimizing the design shipped atomically with the design. | Cite the timestamped audit artifact for the contract draft in plan.md L32 so the ordering is evidenced. Going forward, land constitution amendments in their own PR before dependent code (Governance §Amendment step 1). |
| I1 | HIGH | SC-007 and the US3 checkpoint claim flat mode is 'byte-identical in names and contracts to 0.3.x', and plan.md L21/L78/L94 claim per-area files are untouched. T030/T031 added identifier and revision regexes to IAM and secret-manager schemas and T029 changed three handlers (git diff confirms 5 files under src/tools changed), so flat-mode input contracts are tightened, not identical. | Amend SC-007/US3-AS1 to 'unchanged names and contracts except the documented tightening of IAM identifier and secret revision formats'; correct plan.md L21, L78, L94 and tasks.md L124 accordingly (CHANGELOG already discloses the tightening). |
| I2 | HIGH | FR-017 says validation errors MUST NOT echo submitted values, and FR-013 says every restriction applies identically to the flat surface; but flat/both mode uses the SDK's native validation, which echoes values (verified probe: scaleway_dns_list_records returned `"received": "SECRET_VALUE_X"`). SC-007/US3 simultaneously require legacy behaviour unchanged. The spec does not resolve this conflict. | Scope FR-017 explicitly to the gateway surface and add an edge case plus README note that flat mode retains SDK-native validation errors, or route flat registrations through the same validationError shaping (a deliberate legacy-behaviour change). Pick one and record it. |
| I3 | HIGH | Two error envelopes coexist: gateway pre-dispatch errors are `{error: string, op, issues\|suggestions}` while forwarded handler errors are `{error: {type, message, statusCode}}`. In both mode one client sees both shapes. Constitution III requires error codes/messages consistent across all tools; FR-017/FR-019 and the contract never specify the envelope. | Define the gateway error envelope in contracts/gateway-tools.md aligned with ApiError (type in invalid_input\|not_found\|permission_denied\|server_error, message, plus op/issues/suggestions) and add a contract assertion; update FR-017/FR-019 to reference it. |
| A1 | HIGH | SC-005 'on the order of a few thousand tokens ... at least two orders of magnitude less' has no defined measurement (model, workflow, count method). T019 measures listing bytes only; T055 (token measurement) is blocked on a 503. The criterion is currently unverifiable. | Restate SC-005 as a byte budget for a scripted search→describe→read sequence (extend scripts/measure-discovery.ts to emit it) with the token figure marked informational until T055 completes. |
| G1 | HIGH | FR-026 (flat mode supported for the entire 0.x series; removal requires a major bump preceded by a deprecation minor) has zero tasks and no user-facing statement: grep of README.md and CHANGELOG.md finds no '0.x', 'deprecation' or 'major version' text. T038 does not cover it. This is the commitment US3 relies on. | Add a task to document the support window in README (Server-side filters section) and the next CHANGELOG entry; optionally add a unit test asserting ModeSchema still accepts 'flat' while package major version is 0. |
| A2 | MEDIUM | SC-004 'natural keyword queries' is undefined; the 20-case smoke set is identifier-shaped ('instances list servers'). FR-006 requires every keyword to match with no normalization, so 'list kubernetes cluster' (singular) does not return k8s_list_clusters (verified: returns k8s_list_cluster_available_versions, k8s_list_pools). | Define 'natural' (e.g. at least half the cases must not be token permutations of the identifier) and add light normalization (plural stripping) to FR-006, then extend the smoke set. |
| U1 | MEDIUM | lookupError reuses the all-tokens-must-match search, so typos yield zero suggestions ('instances_lst_servers' → [], 'rdb_list_databse' → [], verified). FR-018 'up to five' is satisfied vacuously, but the US4 independent test expects suggested identifiers. The similarity rule is unspecified. | Specify the fallback in FR-018 and data-model (e.g. partial-token overlap or edit distance when exact-token search is empty) and add a typo case to tests/contract/gateway.test.ts. |
| I4 | MEDIUM | Spec and data-model say endpoints are declared in the parity record with composites joined by ' + ', but route-guard.ts hardcodes the IAM rule legs by label because the matrix records them as 'PUT /iam/v1alpha1/rules (SetRules read+set: GET ... then PUT ...)'. Enforcement therefore has a second source of truth that FR-023's drift gate does not cover. | Express the three IAM composites in tests/parity-matrix.json using the ' + ' form, regenerate operations.json, delete the label-specific override, and add a test asserting no per-label overrides exist in parseApiMatchers. |
| I5 | MEDIUM | T024 says the filter-bypass protocol test lives in tests/unit/server.test.ts; the actual test ('cannot bypass filters in %s mode with call/read or legacy names') is in tests/contract/gateway.test.ts. server.test.ts only checks one both-mode call. | Fix the T024 file reference to tests/contract/gateway.test.ts (traceability for SC-006). |
| G2 | MEDIUM | FR-015 requires preset membership to be 'documented'; README enumerates `core` but sends users to src/shared/toolsets.ts for the ten family presets, contradicting the US3 spirit of 'without consulting source code'. (Partition property verified: the ten families cover all 50 areas with no overlap.) | Enumerate family memberships in README (or generate the table from TOOLSET_PRESETS in a doc task) and note that changes are release-noted. |
| G3 | MEDIUM | SC-002 (listing size unchanged when operations are added/removed) has no direct assertion; tests only bound bytes (< 12,000). | Add a test comparing listing bytes for the 5-operation fixture registry and the full registry (must be equal), or demote SC-002 to a design note. |
| G4 | LOW | FR-027/SC-009 (zero upstream requests for discovery) are enforced only by the fetch stub in gateway.test.ts; no task description names them. | Mention FR-027/SC-009 in T012 so the traceability is explicit. |
| I6 | LOW | Catalog size drifts between 733 (audit baseline, 0.3.x) and 724 (shipped after feature 060 removals); SC-001's baseline is the 733-tool listing while the 99.61% figure is measured against 724-tool flat mode. | State once (plan Summary or validation.md) that 733 became 724 after 060 removals and that the 99.61% figure is versus 724. |
| I7 | LOW | Stale pre-release statements remain in artifacts of a published release (0.4.0/0.4.1). | Remove or date these lines. |
| D1 | LOW | FR-027 restates Clarification Q4 and SC-009; edge case L96 restates FR-011 and Clarification Q1; FR-026 restates Clarification Q3. | Keep the FRs normative and reduce the Clarification/edge-case copies to pointers. |
| I8 | LOW | Terminology drift by design (spec avoids implementation names) but no mapping is given: identifier vs op/operation ID; compact/flat/combined vs gateway/flat/both; area vs toolset. | Add a one-line glossary under Key Entities mapping spec terms to runtime names. |
| I9 | LOW | Spec entity 'Endpoint Declaration' is named RouteContext/RouteMatcher in data-model; data-model adds Preset and ValidationErrorResult that are absent from spec Key Entities. | Align names or add cross-references in both files. |
| U2 | LOW | Six tasks map to no requirement or story. T037 (library entry does not start stdio) and T047 (packed install runs in all modes) are release-quality properties the spec never states. | Add an SC for 'package importable without starting stdio; packed install runs in all three modes', or mark T003/T011/T039/T045 explicitly as infrastructure. |
| C7 | LOW | Principle VI 'unit tests under 5 s': measured here 4.18 s without coverage (passes) and 5.48 s for the CI invocation with coverage. The clause as worded is met; headroom is thin and the plan's 4.46 s figure omits the coverage run. | Record both figures in plan.md L35 and watch the budget as gateway tests grow. |

## Evidence provenance

Workflow result `w7uquajh2` from this session; report persisted here so temporary audit files are not a dependency.
The first 059 confirmation failed with provider-pool HTTP 503. Its missing result was not a successful clearance.
