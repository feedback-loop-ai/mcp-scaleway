# Initial analysis record

Historical read-only analysis performed during the 2026-09-06 retrofit.
Counts describe that pass, not the final state. See analysis.md and ../retrofit-compliance.md.
No historical reviewer statement grants a governance exemption.

| ID | Severity | Finding | Recommendation |
| --- | --- | --- | --- |
| C1 | CRITICAL | Constitution III MUST: 'Tool schemas MUST be defined in contracts/ before any code is written'. This feature rewrote tool schemas (src/tools/autoscaling/types.ts +643 lines, src/tools/containers/types.ts +122 in ce01175) but has no contracts/ directory; plan's Project Structure omits it and the III=PASS row cites only specs/scaleway-api/*.md. The original tool contracts still describe the retired surfaces (v1alpha1 + instance-policy tools; containers v1beta1 with deploy/token), so the only contracts/ artifacts for these tools now contradict the shipped code. | Add specs/060-api-correctness/contracts/{autoscaling,containers,elastic-metal}-tools.md capturing the migrated tool schemas (or supersede the 045/008 contracts with a pointer), and change plan.md III to 'EXCEPTION (documented)' with the Governance justification until they exist. Do not leave III=PASS. |
| C2 | CRITICAL | Constitution VIII MUST: 100% coverage 'across all source files. No exceptions, no exclusions'. tests/vitest.config.ts excludes src/main.ts from coverage. plan.md declares VIII PASS ('100% lines/branches') and SC-007 asserts 100% without disclosing the exclusion. Pre-existing since a71f26c, but the Constitution Check is the place it must be surfaced; silence here is dilution. | Either cover src/main.ts (e.g. tests/unit/main.test.ts driving startServer via vi.mock) and remove the exclude, or record the exclusion as a documented exception in plan.md VIII with a follow-up task; a permanent carve-out requires a constitution amendment outside /speckit.analyze. |
| C3 | CRITICAL | Constitution I MUST: 'Tool descriptions MUST be explicit, action-oriented, and include usage examples'. This feature authored new descriptions for all autoscaling and containers tools and six cockpit tools; none contains a usage example (grep 'example' = 0 in all four index files). plan.md labels the gap 'pre-existing' and defers to 059 T058, but descriptions written in ce01175 are not pre-existing. | Add a one-line usage example to each description rewritten in this feature (autoscaling, containers, cockpit), or re-label plan.md I as 'PARTIAL (exception documented)' with the 059 T058 reference and an approver; 'PASS' is inaccurate. |
| C4 | HIGH | Principle II ('Every feature MUST have a spec before implementation begins') was violated and is honestly recorded in Complexity Tracking, which satisfies Governance ('Violations MUST be documented with justification'). But the gate row still reads 'PASS (retrofitted)', contradicting its own evidence cell, and L95 claims the retrofit was performed 'with clarify and analyze passes' while T049 (run /speckit.analyze) is unchecked. | Change the II row to 'EXCEPTION (documented, owner directive 2026-09-05)'; reword L95 to say the analyze pass is pending until T049 is checked. |
| U1 | HIGH | FR-014 requires every repaired or migrated area to have a test driving the real client transport with injected HTTP; Q3 says mocking client.fetch 'does not count'. Autoscaling (a migrated area) has only schema-parse contract tests plus unit tests that mock client.fetch, and appears in no path-auth case. key-manager, secret-manager, edge-services and sns (repaired areas, T019) are proven via fakeClient{fetch: mockFetch}. T022 and T012 are checked as if they satisfy FR-014. | Add autoscaling cases to tests/contract/transport/path-auth.contract.test.ts (or an autoscaling.transport.test.ts) built with createAdvancedClient; do the same for one list op per generated-client area so page_size is observed on the wire. Until then, uncheck T022/T012 or annotate them as partial. |
| U2 | HIGH | SC-001 claims 100% of operations are 'verified automatically across the whole catalog' and plan L37 says real-transport tests were 'added for the whole catalog'. The in-repo test covers 17 GET operations plus 6 error statuses on one op; the whole-catalog harness lives at /tmp/scw-audit/smoke/smoke.ts, is not committed, and never runs in CI. The '724 OK, 0 BAD_URL, 0 NO_AUTH' checkpoint is unreproducible from the repository. | Commit the harness as tests/contract/transport/catalog-smoke.contract.test.ts (synthesized inputs, fail-closed fetch, assert origin/leading slash/x-auth-token for every registered op), or reword SC-001 and plan L37 to '17 representative operations' and move the whole-catalog claim to T052. |
| U3 | HIGH | SC-002 ('0 registered operations target an upstream version that upstream reports as not-found') and the US2 Independent Test ('enumerate ... against current upstream API references') can only be verified against the live service, which the spec excludes from automated tests (L89, Assumptions). The only path to verify is T052, an unchecked manual follow-up. The parity gate checks 'METHOD /path' format, not upstream liveness. | Either define the automated proxy (e.g. a test asserting every matrix api path's version segment is in an allow-list maintained in specs/scaleway-api/README.md, updated when upstream retires a version) or restate SC-002 as verified by the manual smoke and make T052 a required checkpoint rather than an optional follow-up. |
| I1 | HIGH | FR-010 says unsupported combinations 'return an explicit error' but never defines it. The implementation emits type 'unsupported_operation' with statusCode 501, a type absent from the shared ApiErrorType enum and from the data-model Error Envelope (which lists five types and derives statusCode from upstream). Constitution III requires error codes to be consistent across all tools; this is a per-area invented error class. | Add 'unsupported_operation' (501) to ApiErrorType and to data-model.md's Error Envelope, and name the type in FR-010; alternatively map it to invalid_input (400) with a suggestion field. Add a unit test in tests/unit/shared/errors.test.ts once it is part of the shared contract. |
| A1 | MEDIUM | 'Protocol-valid acknowledgement' is never given a shape. Implementations diverge: InterLink returns {message: 'Routing policy deleted successfully'}, elastic-metal returns {} for every undefined result. data-model says 'acknowledgement object' without fields. | Define one acknowledgement shape in data-model.md (e.g. {deleted: true, id}) and cite it from FR-005; align the two handlers and their tests. |
| I2 | MEDIUM | Plan attributes api-reference updates for product-catalog, iam and secret-manager, and 'new schema constraints for IAM identifiers, secret revisions', to this feature. git log shows product-catalog api-reference last changed in #53 (54a75b1), iam in a6ca23b and secret-manager in f46a252, both part of feature 059 / PR #55, not PR #54. | Trim plan L32/L56 to autoscaling, containers, elastic-metal, public-gateway, cockpit; move the IAM/secret constraint evidence to 059's plan or mark it 'landed via 059'. |
| E1 | MEDIUM | The edge case exempting object storage and generative APIs from shared-transport rules 'but must still send correct authentication' has no task and no test in this feature; nothing asserts SigV4 or api.scaleway.ai bearer auth under the new proof standard. | Add a task: real-transport test for one object-storage op (Authorization: AWS4-HMAC-SHA256 header present) and one generative-API op, or move the sentence to Out of Scope with a follow-up. |
| E2 | MEDIUM | SC-006 says agreement between parity record, API references, README and release notes is 'verified by automated parity checks and a documentation cross-check'. The cross-check is an inline python snippet in quickstart.md, not a test or CI step; it passes today (724/724) but nothing prevents drift. No task automates it. | Add tests/unit/docs-parity.test.ts asserting every matrix tool name appears in README.md (and the four gateway tools are the only extras), and cite it from SC-006. |
| I3 | MEDIUM | Plan claims 'new contract tests referenced from the matrix'. True for flexible-ip, containers and autoscaling files, but no matrix entry references the path-auth real-transport test, so the 17 areas whose transport proof lives there have matrix rows pointing only at older schema-level tests. Constitution VIII's parity matrix is meant to map each operation to the test that proves it. | Either allow a contract_test array per entry and add path-auth to the 17 covered ops, or keep the matrix single-valued and note in plan L37 that path-auth is a cross-cutting regression not tracked per operation. |
| E3 | LOW | T024 says 'assert absence' of DHCP; no test mentions dhcp. Absence is implied only by the exact 21-name list in the contract test. Acceptable, but the task wording overstates. | Either add an explicit expect(names.some(n => n.includes('dhcp'))).toBe(false) or reword T024 to 'assert exact 21-tool registration'. |
| I4 | LOW | Feature Branch is declared as 060-api-correctness but no such branch exists (work shipped via docs/spec-retrofit and PR #54's branch); check-prerequisites.sh aborts on main. Constitution's feature/###-name convention is not followed. | Record the actual delivery branches in plan.md header, or create the branch for the retrofit PR so speckit tooling resolves FEATURE_DIR. |
| D1 | LOW | FR-008 (remove rather than stub, with migration note) and FR-010 (no silent semantic substitution; explicit error or removed) overlap on the removal clause. | Keep FR-008 as the removal rule; narrow FR-010 to the 'explicit error for unsupported combinations' case and cross-reference FR-008. |
| A2 | LOW | Impact counts drift across artifacts. git at 0006056 shows 14 unslashed areas in T014's list plus autoscaling (fixed wholesale in T026), reconciling 14 vs 15, but the 290 vs 220 figures are never reconciled. | State one breakdown once (e.g. in research.md Decision 2) and reference it from spec and tasks. |
| T1 | LOW | Spec uses 'Parity Record', 'Shared Transport', 'Operation'; plan/tasks/constitution use 'parity matrix', 'SDK client / shared client / advanced client', 'tool'. Same concepts, four names. | Add a one-line glossary to spec Key Entities mapping each term to its implementation name. |
| E4 | LOW | Edge case says only endpoints documented as no-content are normalised; elastic-metal's apiRequest normalises every undefined result to {} regardless of documentation. Behaviourally harmless (a body is returned when present) but the spec statement is not what the code does. | Reword the edge case to 'empty upstream bodies are normalised to an acknowledgement; documented bodies are returned as-is'. |
| U4 | LOW | T050 (adopt official product SDKs for rdb/instances) is listed as a task in this feature while spec explicitly places it Out of Scope. Constitution V: 'if it's not in the spec, don't build it'. | Move T050 to a follow-ups note outside the numbered task list, or open a separate feature spec. |

## Evidence provenance

Workflow result `wfgw22e73` from this session; report persisted here so temporary audit files are not a dependency.
The first 059 confirmation failed with provider-pool HTTP 503. Its missing result was not a successful clearance.
