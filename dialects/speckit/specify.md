# spec-kit specify

Write `specs/<change>/spec.md` from the repository template: user stories, functional requirements, edge cases, and measurable success criteria. Mark a genuinely open decision as `[NEEDS CLARIFICATION: …]`. On return, encode every answer in the specification.
