# spec-kit tasks

Write `specs/<change>/tasks.md` as phased checkbox rows using `[ID]`, optional `[P]`, and `[Story]` labels. Make dependencies and file paths explicit. On return, repair every cited coverage or consistency gap.
